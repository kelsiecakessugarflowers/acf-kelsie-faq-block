# FAQ-ACF-Block
Registers custom ACF blocks without a child theme to be used with Rankmath FAQ Schema. Schema output is intentionally disabled in this plugin to avoid duplicate FAQ markup when another Rank Math integration handles it.
